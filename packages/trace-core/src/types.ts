/**
 * Compatibility re-export.
 *
 * The vocabulary moved to `@begame/trace-spec` so `@begame/core` can use it
 * without depending on this package. Kept so both `@begame/trace-core` and
 * `@begame/trace-core/types` keep exporting exactly what they did before.
 */
export * from "@begame/trace-spec";