[**BEGame**](../README.md)

***

# Class: InfoScoreboard

> **Deprecated:** 使用 [SidebarScoreboard](SidebarScoreboard.md)。

`InfoScoreboard` 是 `SidebarScoreboard` 的兼容别名；旧 `scoreBoardName` 拼写仍被接受，新代码使用 `scoreboardName`。
