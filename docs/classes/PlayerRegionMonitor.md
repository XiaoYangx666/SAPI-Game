[**BEGame**](../README.md)

***

# Class: PlayerRegionMonitor

> **Deprecated:** 使用 [RegionBoundary](RegionBoundary.md) + `regionBoundary(options)`。

旧 `players:` / `groups:` 二选一配置仍由兼容适配器支持，底层完全复用 `RegionBoundary`。
